{
  "name": "@user-search/core",
  "version": "0.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --outDir dist",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest --config ../../jest.packages.config.js"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
